# 📚 PortionPal API Documentation

Dokumentasi API lengkap untuk PortionPal, di-generate dari JSDoc comments menggunakan TypeDoc.

## 🚀 Cara Menggunakan

### Generate Dokumentasi

```bash
# Generate dokumentasi
npm run docs

# Generate dokumentasi dengan watch mode (auto-reload saat ada perubahan)
npm run docs:watch
```

### Membuka Dokumentasi

1. Buka file `docs/index.html` di browser
2. Atau serve dengan local server:
   ```bash
   # Menggunakan Python
   python -m http.server 8000 -d docs
   
   # Menggunakan Node.js (http-server)
   npx http-server docs -p 8000
   ```

## 📖 Struktur Dokumentasi

Dokumentasi ini mencakup:

- **Utilities** (`src/lib/`) - Utility functions dan helpers
- **Hooks** (`src/hooks/`) - Custom React hooks
- **Components** (`src/components/`) - React components
- **Types** (`src/types/`) - TypeScript type definitions

## 🔍 Fitur

- ✅ **JSDoc Comments** - Semua fungsi publik memiliki dokumentasi lengkap
- ✅ **Type Information** - Parameter dan return types terlihat jelas
- ✅ **Examples** - Contoh penggunaan untuk fungsi kompleks
- ✅ **Search** - Pencarian cepat untuk menemukan fungsi
- ✅ **Navigation** - Navigasi mudah dengan sidebar

## 📝 Menambahkan Dokumentasi

Untuk menambahkan dokumentasi baru, gunakan format JSDoc:

```typescript
/**
 * Deskripsi singkat fungsi
 * 
 * Deskripsi lengkap jika diperlukan
 * 
 * @param paramName - Deskripsi parameter
 * @returns Deskripsi return value
 * @throws Deskripsi error yang mungkin terjadi
 * 
 * @example
 * ```ts
 * // Contoh penggunaan
 * const result = myFunction('param');
 * ```
 */
export function myFunction(paramName: string): string {
  // implementation
}
```

## 🔄 Update Dokumentasi

Setiap kali ada perubahan pada kode, jalankan:

```bash
npm run docs
```

Untuk update dokumentasi.

---

**Generated by:** TypeDoc  
**Last Updated:** $(date)

